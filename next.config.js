/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['placekitten.com', 'picsum.photos', 'placebear.com'], // add other domains as needed
  },
};

module.exports = nextConfig;
